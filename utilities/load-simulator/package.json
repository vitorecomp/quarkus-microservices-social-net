{
  "name": "y",
  "version": "1.0.0",
  "description": "Container enabling the renovation or download of a ZeroSSL certificate with google domains",
  "main": "index.js",
  "scripts": {
    "generate-cert": "export NODE_ENV=production && node dist/main.js",
    "dev-test": "export NODE_ENV=dev && ts-node main.ts",
    "build": "export NODE_ENV=dev && tsc",
    "build-run": "export NODE_ENV=dev && tsc && npm generate-cert"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "winston": "^3.8.2",
    "zerossl": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
